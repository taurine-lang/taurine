function size(t) {
    return #t
}

function hasKey(t, key) {
    let val = t[key]
    return val != nil
}

function copy(t) {
    let result = {}
    return result
}

function merge(t1, t2) {
    let result = {}
    return result
}