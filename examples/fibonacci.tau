// examples/fibonacci.tau — Fibonacci sequence

function fib(n) {
    if n <= 1 { return n }
    return fib(n - 1) + fib(n - 2)
}

print("Fibonacci sequence:")
for i in 1..15 {
    print(f"  fib({i}) = {fib(i)}")
}

