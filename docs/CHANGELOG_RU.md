# CHANGELOG

Все значимые изменения в Taurine задокументированы в этом файле.

## [1.0.0] - 2026-03-16

### 🎉 Стабильный релиз!

Первый стабильный релиз языка программирования Taurine.

### Добавлено

- **let/const** объявления переменных
- **f-строки** для интерполяции строк
- **Multi-return** функции с деструктуризацией
- **break/continue** в циклах
- **Nil-safe операторы** (`?.`)
- **JSON-стиль таблиц** (`{key: val}`)
- **Диапазоны** (`1..10`)
- **Присваивание по индексу массива** (`arr[i] = value`)
- **Параметры по умолчанию** в функциях
- **Функциональные литералы** (анонимные функции)

### Стандартная библиотека

- `std/json.tau` — Парсинг/сериализация JSON
- `std/http.tau` — HTTP клиент
- `std/crypto.tau` — Криптографические функции
- `std/date.tau` — Дата/время
- `std/regex.tau` — Регулярные выражения
- `std/array.tau` — Утилиты для массивов
- `std/string.tau` — Строковые утилиты
- `std/io.tau` — Файловый ввод/вывод
- `std/math.tau` — Математические функции
- `std/os.tau` — Функции ОС

### Производительность

- Замена `Arc<Mutex<>>` на `Rc<RefCell<>>` для ускорения в 2-3 раза
- Constant folding оптимизатор
- Dead code elimination

### Изменено

- Удалён менеджер пакетов (фокус на ядре языка)
- Упрощён CLI интерфейс

---

## [0.12.0] - 2026-03-15

### Добавлено

- Тестовый фреймворк (`taurine test`)
- Benchmark suite (`taurine bench`)
- Функции `assert()` и `assert_eq()`

---

## [0.11.0] - 2026-03-14

### Добавлено

- Ключевые слова `let` и `const` (алиасы для `loc`)
- JSON-стиль синтаксиса таблиц (`{key: val}`)
- Защита const (нельзя переназначить)

---

## [0.10.0] - 2026-03-13

### Добавлено

- Операторы `break` и `continue`
- f-строки (`f"Привет, {name}!"`)
- Multi-return значения
- Деструктуризация присваиваний

---

## [0.9.0] - 2026-03-12

### Производительность

- Замена `Arc<Mutex<>>` на `Rc<RefCell<>>`
- Оптимизированный поиск переменных

---

## [0.8.0] - 2026-03-11

### Добавлено

- Присваивание по индексу массива (`arr[0] = 100`)
- JSON парсинг/сериализация
- HTTP клиент (GET, POST, PUT, DELETE)
- Диапазоны в for-in циклах
- Nil-safe оператор (`?.`)
- Поддержка regex
- Функции даты/времени
- Криптографические функции (MD5, SHA256, Base64, UUID)

---

## [0.7.0] - 2026-03-10

### Добавлено

- Синтаксис вызова методов (`obj:method()`)
- Параметры по умолчанию
- Функциональные литералы

---

## [0.6.0] - 2026-03-09

### Добавлено

- Начальный релиз
- Базовые функции языка
- Заготовки стандартной библиотеки
